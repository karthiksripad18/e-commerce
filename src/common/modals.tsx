export interface userState {
    userName: string | null;
    userEmail: string | null;
};

export interface productModal {
    id: number;
    title: string;
    price: string;
    category: string;
    description: string;
    image: string;
};